[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Festnageln, damit der Build nicht irgendwann an einem Node-Wechsel scheitert.
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
